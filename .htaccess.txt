# Activation de la réécriture d'URL
RewriteEngine On

# Redirection vers HTTPS
RewriteCond %{HTTPS} !=on
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Redirection vers une URL avec www (décommente si besoin)
# RewriteCond %{HTTP_HOST} !^www\. [NC]
# RewriteRule ^ https://www.%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Suppression du trailing slash sauf pour les dossiers
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} (.+)/$
RewriteRule ^ %1 [R=301,L]

# Forcer l'encodage UTF-8
AddDefaultCharset UTF-8

##########################################
# Sécurité de base
##########################################

# Empêche l'accès aux fichiers sensibles
<FilesMatch "\.(htaccess|env|git|gitignore|DS_Store)$">
  Order Allow,Deny
  Deny from all
</FilesMatch>

# XSS protection & autres headers utiles
<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-XSS-Protection "1; mode=block"
  Header set X-Frame-Options "SAMEORIGIN"
  Header always set Referrer-Policy "no-referrer-when-downgrade"
  Header always set Permissions-Policy "camera=(), microphone=(), geolocation=()"
</IfModule>

##########################################
# Compression GZIP (mod_deflate)
##########################################

<IfModule mod_deflate.c>
  # Compression HTML, CSS, JavaScript, JSON, XML
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE text/javascript
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE application/xhtml+xml

  # Compression des polices
  AddOutputFilterByType DEFLATE font/ttf
  AddOutputFilterByType DEFLATE font/woff
  AddOutputFilterByType DEFLATE font/woff2
  AddOutputFilterByType DEFLATE application/font-woff2

  # Compression SVG
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>
##########################################
# Mise en cache des ressources statiques
##########################################

<IfModule mod_expires.c>
  ExpiresActive On

  # Images (6 mois)
  ExpiresByType image/avif "access plus 6 months"
  ExpiresByType image/webp "access plus 6 months"
  ExpiresByType image/jpeg "access plus 6 months"
  ExpiresByType image/png "access plus 6 months"
  ExpiresByType image/gif "access plus 6 months"
  ExpiresByType image/svg+xml "access plus 6 months"

  # Fonts (6 mois)
  ExpiresByType font/woff2 "access plus 6 months"
  ExpiresByType font/ttf "access plus 6 months"
  ExpiresByType application/font-woff2 "access plus 6 months"

  # CSS & JS (1 mois)
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"

  # Par défaut
  ExpiresDefault "access plus 1 month"
</IfModule>

<IfModule mod_headers.c>
  <FilesMatch "\.(avif|webp|jpg|jpeg|png|gif|svg|js|css|ttf|woff2)$">
    Header set Cache-Control "public, max-age=15552000"
  </FilesMatch>
</IfModule>

##########################################
# Anti-hotlinking (empêche les autres sites d'utiliser tes images)
##########################################

RewriteEngine on
RewriteCond %{HTTP_REFERER} !^$
RewriteCond %{HTTP_REFERER} !^https?://(www\.)?fleurs-story\.fr [NC]
RewriteRule \.(jpg|jpeg|png|gif|avif|webp)$ - [F,NC,L]

##########################################
# Optionnel : forcer HTTPS + WWW
##########################################

# Décommente ces lignes si tu veux forcer https et www
# RewriteCond %{HTTPS} off [OR]
# RewriteCond %{HTTP_HOST} !^www\. [NC]
# RewriteRule ^ https://www.fleurs-story.fr%{REQUEST_URI} [L,R=301]


# Protection des fichiers sensibles
<FilesMatch "(^\.htaccess|\.htpasswd|config\.php|php.ini|php5.ini|install\.php|wp-config\.php|xmlrpc\.php)">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Personnalisation de la page d'erreur 404
ErrorDocument 404 /404.html
Redirect 301 /page-dupliquee https://www.fleurs-story.fr


<Files "config.php">
  Order Allow,Deny
  Deny from all
</Files>
